One-off / leftover scripts moved here so the repo root stays clean.

- root-oneoffs/   — old fix_*, verify_*, create_* from project root
- server-oneoffs/ — old _tmp_*, fix_*, test_* from server/

Do not run these for normal app use. Real tooling lives in server/scripts/ and npm scripts.
