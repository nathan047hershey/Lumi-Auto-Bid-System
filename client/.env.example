# Client Environment Variables
# Copy to .env and fill in values

# API URL - Leave empty for development (uses relative path)
# For production on Render, this will be set automatically
VITE_API_URL=
